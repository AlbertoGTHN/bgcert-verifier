FROM node:20-alpine
WORKDIR /app
COPY package.json ./
# Install without requiring a lockfile
RUN npm install --legacy-peer-deps
COPY . .
EXPOSE 3000
ENV NODE_ENV=development
ENV HOSTNAME=0.0.0.0
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]
